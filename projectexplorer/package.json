{
  "name": "project-explorer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A public GitHub project explorer with related npm package lookup.",
  "scripts": { "test": "node --test tests/*.test.js" }
}
